const matrix = size => {
    let counter = 1;
    let colStart = 1;
    let colEnd = size;
    let rowStart = 1;
    let rowEnd = size;

    const matrix=[];
    for(let col=colStart-1;col<colEnd-1;col++) {
        
    }

}